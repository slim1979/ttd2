<p id="notice"><%= notice %></p>

<p>
  <strong>Поезд номер:</strong>
  <%= @train.number %>

  <% if @train.vans.empty? %>
    <br />
    К поезду не присоединены вагоны
  <% else %>
  <br /> 
  <strong>Вагоны:</strong>
  <%= render @train.sort_vans, train: @train %>
    <%= form_for(@train) do |form| %>
      <div><%= form.radio_button(:sort_by, "asc") %>В сторону увеличения
                      <%= form.radio_button(:sort_by, "desc") %>В сторону уменьшения
                      <%= form.submit 'Сортировать' %></div>
    <% end %>
</p>
<p>
    Вы можете указать тип вагона и интересующие Вас места для получения информации о них
    <%= form_for(@train) do |form| %>
      <%= form.label :van_type_to_more_info, 'Выберите тип вагона:' %>
      <%= form.collection_select :van_type_to_more_info, @train.vans_collection, :first, :last %>
      <%= form.label :places_type_to_more_info, 'Выберите тип мест:' %>
      <%= form.collection_select :places_type_to_more_info, SeatsKind.all, :seats_type, :title %>
      <p><%= form.submit 'Найти' %></p>
    <% end %>
    <%= @train.adv_info %>
  <% end %>
</p>
<p>
  <strong>Текущий маршрут:</strong>
  <% if @train.route %>
    <%= @train.route.title %>
  <% else %>
    Нет назначенного маршрута
  <% end %>
  <br />
  <strong>Текущая станция:</strong>
  <% if @train.current_station %>
    <%= @train.current_station.title %>
  <% else %>
    Не указано
  <% end %>
</p>

<%= link_to 'Редактировать', edit_train_path(@train) %> |
<%= link_to 'Назад', trains_path %>
