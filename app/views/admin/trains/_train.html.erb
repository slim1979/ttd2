<tr>
  <td><%= train.number %></td>
  <td><%= train.vans.count %></td>
  <td><%= link_to t('links.show'), admin_train_path(train) %></td>
  <td><%= link_to t('links.edit'), edit_admin_train_path(train) %></td>
  <td><%= link_to t('links.delete'), admin_train_path(train), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
