<tr>
  <td><%= train.number %></td>
  <td><%= train.vans.count %></td>
  <td><%= link_to 'Показать', admin_train_path(train) %></td>
  <td><%= link_to 'Изменить', edit_admin_train_path(train) %></td>
  <td><%= link_to 'Удалить', admin_train_path(train), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
